# Complete Family Grouping Validation

The family selector now presents every relationship supported by the current calculation engine without a hidden expansion control. **முதன்மை குடும்பம்** contains spouse, father, mother, sons, and daughters. **மற்ற குடும்பம்** contains paternal grandfather, full brothers and sisters, and maternal brothers and sisters.

The final desktop browser capture confirms that each Tamil label and its counter have independent horizontal space, with no overlap. The same browser journey passed on mobile. The browser test verifies both group headings, the contextual help guide, and the paternal-grandfather control. Type checking, calculation regression tests, local-history tests, and the production build passed.
